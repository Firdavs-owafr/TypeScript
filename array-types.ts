const arr: number [] = [1,2,3,4,5]; 
const matrix: number [][] = [ [], [], [] ]; 
const arr2: (number | string )[] = [1,2,3,'as']; 

arr.map((item) => item + 1);

// Tuples

const cordinats: [number,number] = [12,32];
cordinats.push(98)